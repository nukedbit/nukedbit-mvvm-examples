<?xml version="1.0" encoding="utf-8" ?>
<views:ViewBase x:TypeArguments="viewModels:HomeViewModel" xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:NukedBit.Mvvm.Views;assembly=NukedBit.Mvvm"
             xmlns:viewModels="clr-namespace:MvvmExample.ViewModels;assembly=MvvmExample"
             x:Class="MvvmExample.Views.HomeView">
  <Grid>
    <StackLayout>
      <Label Text="{Binding Title}" HorizontalOptions="Center"></Label>
      <Button Command="{Binding Path=NextCommand}" x:Name="ShowButton" Text="Next" HorizontalOptions="Center" VerticalOptions="Center"></Button>
    </StackLayout>
  </Grid>
</views:ViewBase>